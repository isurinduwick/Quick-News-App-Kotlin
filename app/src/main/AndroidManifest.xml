<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.newsagencyproject">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsAgencyProject"
        tools:replace="android:fullBackupContent"
        tools:targetApi="31">
        <activity
            android:name=".EditorNewsDetails"
            android:exported="false" />
        <activity
            android:name=".NewsDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".EditorActivity"
            android:exported="false" /> <!-- SignIn Activity (Launcher) -->
        <activity
            android:name=".SignIn"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Firebase Ads Meta-data -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-5949911435320023~7592817501" /> <!-- SignUp Activity -->
        <activity
            android:name=".SignUp"
            android:exported="true" /> <!-- Home Activity -->
        <activity
            android:name=".Home"
            android:exported="false" /> <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true" /> <!-- Dashboard Activity -->
        <activity
            android:name=".DashboardActivity"
            android:exported="false" /> <!-- Publisher Activity -->
        <activity
            android:name=".PulisherActivity"
            android:exported="false" />
    </application>

</manifest>